<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="point">
	<sql id="search">
		SELECT * FROM point ORDER BY pointnum		
	</sql>
	
<!-- 	<select id="total" resultType="Integer"> -->
<!-- 		SELECT COUNT(*) FROM  -->
<!-- 			(<include refid="search"></include>) sub -->
<!-- 	</select> -->
	
	<select id="list" resultType="Point">
		SELECT * FROM   			
    		(<include refid="search"></include>) sub
<!--     	LIMIT #{offset}, #{perPage} -->
	</select>
	
	<insert id="add">
		INSERT INTO point
		(id, name, address, phone, email)
		VALUES (#{pointid}, #{name}, #{address}, #{phone}, #{email})
	</insert>
	
	<delete id="delete">
		DELETE FROM point
		WHERE pointnum=#{pointnum}
	</delete>
	
	<update id="update">
		UPDATE point
		SET pointid=#{pointid}, name=#{name}, address=#{address}, phone=#{phone}, email=#{email}
		WHERE pointnum=#{pointnum}
	</update>
	
	<select id="item" resultType="Point">
		SELECT * FROM point
		WHERE pointnum=#{pointnum}
	</select>
</mapper>